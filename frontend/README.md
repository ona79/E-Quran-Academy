# Frontend — E-Quran Academy

Next.js (App Router). Un dossier par espace utilisateur :
`(eleve)`, `(professeur)`, `(admin)` — chacun avec ses propres pages,
plus `composants/` pour ce qui est partagé entre les trois.
